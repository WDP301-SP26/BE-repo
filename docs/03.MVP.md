Login + phân quyền cơ bản (Admin/Staff/Accountant)

Quản lý doanh nghiệp/khách hàng

Tạo + quản lý hoá đơn (draft → issued → cancelled)

Tính thuế theo rule đơn giản (VAT, tổng hợp)

Xuất PDF/Excel (hoặc template)

Tích hợp cổng thanh toán (vd VNPAY) + xử lý trạng thái giao dịch

Audit log, lịch sử thay đổi, chuẩn hoá trạng thái nghiệp vụ

Dashboard/report cơ bản

Deploy + demo flow end-to-end

Risk:

Nghiệp vụ hoá đơn/thuế (BR)

Tích hợp bên thứ ba (thanh toán/hoá đơn điện tử) + xử lý lỗi + retry

Báo cáo & đối soát (dữ liệu phải sạch và đúng
